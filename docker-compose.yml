services:
  db:
    image: postgres:18
    container_name: postgres_container_dev
    environment:
      POSTGRES_USER: job_user
      POSTGRES_PASSWORD: thisIsASecret123
      POSTGRES_DB: jobs
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
